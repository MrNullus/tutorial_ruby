# | Operadores Logicos |
# * São dois principais:
# ? && (and) e || (or)

# ? Outros operadores:
puts "not => operador de negação"
puts "and e or => mesmo valor que && e ||, são apenas chamados de operadores de composição"
puts "& => operador 'e' bit a bit em valores inteiros"
puts "^| => operador 'ou exclusivo' para valores inteiros"
puts "<= == >= => ! operadores de comparação"
puts "< = > == === != =~ !~ => operadores de igualdade e reconhecimento de padrões"
